import type { GoodId } from '../../types/Goods';

/**
 * Stable order for goods display and processing.
 * This ensures consistent ordering across the UI.
 */
export const GOOD_ORDER: GoodId[] = ['fish', 'wood', 'ore'];
